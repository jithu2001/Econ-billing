# Registration token required by the in-app Sign Up screen.
# Read at startup via os.Getenv("REGISTRATION_TOKEN"); falls back to a
# built-in default if unset. Set this in the shell that launches the app.
REGISTRATION_TOKEN=919847073856
